[Unit]
Description=web app service
After=network.target

[Service]
User=webapp
Group=webapp
WorkingDirectory=/home/pi/pi-web
ExecStart=/home/pi/pi-web/.venv/bin/uvicorn main:app --host 0.0.0.0 --port 8080
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
